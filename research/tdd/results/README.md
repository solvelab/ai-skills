# Stamps

Probe records and stripped exports land here, named `YYYYMMDD-HHMMSS-<kind>.json`:
`probe` for an isolation probe, `export` for a `--report --export` aggregate.

Empty until the first paid run.
